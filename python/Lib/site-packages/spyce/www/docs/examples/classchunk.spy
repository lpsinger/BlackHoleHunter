[[!
def __init__(self):
    self.i = int(request.getpost1('i', 0))

def plusone(self):
    self.i += 1

def plusother(self, api, n):
    self.i += int(n)
]]

<spy:parent title="Active Handler example" />
<f:form>
    [[= self.i ]]
    <br>
    <f:hidden name=i value="=self.i" />
    <f:submit handler=self.plusone value="Add one" />
    <br>
    <f:text name=n />
    <f:submit handler=self.plusother value="Add custom" />
</f:form>
